name: levne_opravy
description: Levné Opravy - Digitální stavbyvedoucí do kapsy. Flutter UI prototyp.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  google_fonts: ^6.1.0
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # Uncomment to use local assets:
  # assets:
  #   - assets/images/
  #   - assets/icons/

  # Using default Material fonts (Roboto)
  # Custom fonts can be added here if needed
